function mout = compute_avg_overlaps(m,v,pct_correct,corr_thresh)

if nargin < 2
	pct_correct = false;
end

if pct_correct
    tmp=mean(m >=corr_thresh,2);
else
    tmp=mean(m_n,2);
end

mout(:,:,1) = tmp(v(1));
mout(:,:,2) = mean(tmp(v(2:Nv)));
mout(:,:,3) = mean(tmp(setdiff(1:P,v)));